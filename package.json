{
  "name": "reaktorihaaste",
  "version": "1.0.0",
  "description": "",
  "engines": {
    "node": "10.x"
  },
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "watch": "nodemon backend/index.js",
    "start": "node backend",
    "build": "npm install && cd react-frontend/ && npm install && npm run build",
    "dev": "start npm run watch && cd react-frontend && start npm run start"
  },
  "cacheDirectories": [
    "node_modules",
    "react-frontend/node_modules"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.3"
  },
  "devDependencies": {
    "jest": "^24.9.0",
    "nodemon": "^2.0.2"
  }
}